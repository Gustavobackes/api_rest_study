server:
  port: 8183

spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
    h2:
      console:
        enabled: true
        path: /h2-console

# application:
feign:
  client:
    config:
      email-service:
        url: http://localhost:8083